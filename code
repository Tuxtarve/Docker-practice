# Docker 실습 기록

## 프로젝트 목표
- Docker 이미지 다운로드 -> 컨테이너 실행 -> 테스트
- VSCode 환경에서 문제 해결 과정 기록


## 디렉토리 구조
- 'src/' : Dockerfile, index.html 등 코드
- 'notes/' : 오류/설명/메모 기록
- 'README.md' : 전체 실습 개요

[2026-02-14 Docker 실습 핵심 요약]

1. GitHub 업로드 루틴 (필수 암기)
   - git add . (한 칸 띄우고 점!)
   - git commit -m "메시지"
   - git push origin main

2. Docker Volume vs Bind-Mount 차이
   - 볼륨(Volume): 도커가 관리하는 영역에 저장. 보안 좋고 깔끔함. (예: DB 데이터)
     명령어: -v 볼륨명:/컨테이너/경로
   - 바인드 마운트(Bind-Mount): 내 맥북 폴더와 직접 연결. 코드 수정 시 바로 반영됨. (예: 소스 코드)
     명령어: -v $(pwd)/폴더명:/컨테이너/경로

3. 트러블슈팅 및 꿀팁
   - Error (No such image): 로컬에 이미지가 없을 때 발생 -> docker pull [이미지명] 먼저 실행.
   - Error (ping: no such file): 이미지(예: Rocky Linux)가 너무 가벼워서 도구가 없는 것 -> dnf install iputils로 설치 필요.
   - 탭탭(Tab-Tab) 자동완성: .zshrc 설정 후 docker rmi [Tab] 누르면 이미지 ID 목록 뜸.
   - 이미지 삭제: ID 전체 안 치고 앞 3글자만 쳐도 삭제 가능 (예: docker rmi a7b).

4. 컨테이너 삭제 절차
   - 정석: docker stop -> docker rm
   - 강제(빠름): docker rm -f [이름]

# 🐳 Docker 사용자 정의 Bridge Network 실습 기록

작성자: Tuxtarve  
날짜: 2026-02-15  
환경: macOS (M3 Pro), Docker Desktop, VSCode

---

# 1. 사용자 정의 네트워크 생성

## 목적
컨테이너 간 이름 기반 통신 및 격리를 위해 사용자 정의 bridge 네트워크 생성

## 명령어

docker network create alpine-net

확인:

docker network ls

---

# 2. 컨테이너 생성 및 네트워크 연결

## alpine-net에 연결된 컨테이너

docker run --name alpine1 -itd --network alpine-net alpine ash
docker run --name alpine2 -itd --network alpine-net alpine ash

## 기본 bridge 네트워크 컨테이너

docker run --name alpine3 -itd alpine ash

## alpine-net에 연결 후 bridge도 추가 연결

docker run --name alpine4 -itd --network alpine-net alpine ash
docker network connect bridge alpine4

---

# 3. 컨테이너 IP 주소 확인

명령어:

docker ps -q | xargs -I {} docker inspect -f '{{.Name}}: {{range .NetworkSettings.Networks}}{{.IPAddress}} {{end}}' {}

결과 예시:

alpine1 → 172.20.0.2
alpine2 → 172.20.0.3
alpine3 → 172.17.0.2
alpine4 → 172.20.0.4, 172.17.0.3

설명:

172.20.x.x → 사용자 정의 네트워크  
172.17.x.x → 기본 bridge 네트워크  

---

# 4. 네트워크 통신 테스트

컨테이너 접속:

docker attach alpine1

---

## 이름 기반 통신 테스트 (성공)

ping -c 2 alpine2
ping -c 2 alpine4

설명:

사용자 정의 네트워크에서는  
컨테이너 이름으로 통신 가능

자동 DNS 지원됨

---

## 다른 네트워크 이름 통신 테스트 (실패)

ping -c 2 alpine3

결과:

ping: bad address 'alpine3'

설명:

다른 네트워크는 이름으로 통신 불가능

---

## IP 기반 통신 테스트 (가능)

ping -c 2 172.17.0.2

설명:

IP 주소로는 통신 가능

---

# 5. alpine4 다중 네트워크 테스트

alpine4는

alpine-net
bridge

두 네트워크 모두 연결됨

IP 2개 보유

따라서 양쪽 네트워크 통신 가능

---

# 6. 외부 네트워크 테스트

ping -c 2 google.com

결과:

정상 통신

설명:

Docker bridge 네트워크는 외부 인터넷 연결 지원

---

# 7. 실습 정리 및 삭제

컨테이너 중지:

docker stop alpine1 alpine2 alpine3 alpine4

컨테이너 삭제:

docker rm alpine1 alpine2 alpine3 alpine4

네트워크 삭제:

docker network rm alpine-net

---

# 8. 핵심 개념 정리

사용자 정의 bridge 네트워크 특징:

- 컨테이너 이름으로 통신 가능
- 자동 DNS 제공
- 네트워크 격리 가능
- Docker 권장 방식

기본 bridge 네트워크 특징:

- 이름 통신 불가능
- IP로만 통신 가능
- 자동 DNS 미지원

---

# 9. 결론

사용자 정의 네트워크는

컨테이너 간 통신
서비스 연결
Docker Compose
멀티 컨테이너 환경

에서 필수적으로 사용됨


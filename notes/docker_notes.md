# Docker Notes

## 오류 기록
- zsh: command not found: tree → Mac 기본 터미널에서 Homebrew 설치 필요
- (base) 프롬프트 문제 → conda config --set auto_activate_base false

## 해결 방법
1. Mac 기본 터미널에서 Homebrew 설치
2. VSCode 터미널 재시작
3. PATH 확인 후 정상 동작

